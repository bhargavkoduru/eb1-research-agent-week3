# Week 3 evaluation

Final model: `Qwen/Qwen3-235B-A22B-Instruct-2507` on Nebius. Tests used public policy questions and synthetic workflow state; no candidate records.

## Results

| Required scenario | Evidence | Outcome |
| --- | --- | --- |
| Successful reviewed save | Live agent searched EB-1B judging policy, prepared specific tasks, paused for review, then saved after simulated approval. | Passed; 12.87 seconds including scripted restart/edit/approval, excluding real human review time. |
| Missing information | Ambiguous job-offer request interrupted for category clarification; cancellation saved no final checklist. Restart-and-clarify is also covered by a deterministic test. | Passed. |
| Unsupported question | Personal approval-probability request handed off with no final save. | Passed. |
| Restart during review, edit/cancel | A new agent instance recovered the review checkpoint; editing required fresh approval. Separate cancel tests confirmed no final save. | Passed. |
| Tool/save failure and recovery | Injected empty retrieval, tool exception, transient database lock and persistent save failure. Failed saves did not report success; retry created one record only. | Passed in deterministic tests. |

The live run additionally verified that its checklist cited the judging passage and contained specific research tasks, rather than generic eligibility notes. See `evals/week3_live.json` for the final trace and checklist. Its approval events were generated by an automated test reviewer in `runtime/week3-live-test/`; they were not real user approvals.

The original combined app passed **28 tests** before the repository split, covering optional access controls, browser workspace isolation, usage limits, actual Streamlit form interactions, exact-draft approval, restarts, editing, cancellation, scope handling, evidence spans and idempotency. Provider/tool failures are injected at the application boundary; this does not simulate every possible network or disk failure.

## Iterations and limitations

The initial smaller model drifted from judging evidence into generic eligibility criteria. A structural save test passed, but source review exposed the irrelevant draft and an overbroad claim. The final test was strengthened to require judging-specific evidence and concrete tasks; the larger model passed. The initial artifact is retained as `evals/week3_initial.json` and must not be presented as final output.

An initial personal-probability request elicited a clarification instead of ending the workflow. The app now explicitly hands off this out-of-scope request before the model runs. Ordinary unclear policy goals still pause for clarification.

Automatic writes to checkpoints and the session catalog preserve unfinished work. User approval gates the final saved checklist. Tasks and user notes are proposed research actions, not verified facts about a candidate. Source citations remain attached to the policy statements when task wording is edited.

All five scenario categories passed in this small test suite. This is not a statistically established production success rate. The final live draft's three policy statements were checked against the attached excerpts by Codex; independent human review is still needed. The manual workflow baseline and actual human review time have not been measured, so no time-savings percentage is claimed.


The standalone Week 3 suite passed **27 tests** locally, retaining the agent, approval, persistence and access-control checks. GitHub Actions validates the standalone repository; the earlier live evaluation artifacts remain unchanged.
